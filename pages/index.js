import Layout from '../components/Layout'
import ProfileCard from '../components/ProfileCard'

const Home = (props) => {
    return (
        <Layout title="Home">
            <ProfileCard />
        </Layout>
    )
}

export default Home;